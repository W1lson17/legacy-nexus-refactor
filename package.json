{
  "private": true,
  "type": "module",
  "scripts": {
    "dev:api": "pnpm --filter @legacy/api dev",
    "dev:web": "pnpm --filter @legacy/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "eslint": "^9.39.4",
    "prettier": "^3.8.3",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma",
      "tsx"
    ]
  }
}