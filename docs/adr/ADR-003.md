# ADR-003: Gestión de Monorepo con pnpm Workspaces

## Context

El proyecto necesita coordinar tres paquetes: `apps/api` (Express backend), `apps/web` (React frontend) y `packages/shared` (tipos y schemas compartidos). Necesitamos un sistema de gestión que permita dependencias internas, ejecución de scripts cross-package y aislamiento estricto entre paquetes.

## Options Considered

1. **pnpm Workspaces**
   - Pros: Aislamiento estricto por defecto — cada paquete tiene su propio `node_modules` con symlinks. El protocolo `workspace:*` permite dependencias internas sin publicar. Comando `--filter` para ejecutar scripts por paquete. Filesystem-efficient con content-addressable store. Ya está en el entorno de desarrollo (pnpm 11.1).
   - Cons: Sin caching de builds inteligente — cada `pnpm build` corre todo. Sin gestión de topological sort automático para builds (aunque `pnpm -r build` lo maneja). Requiere `--filter` explícito para scripts cross-package.

2. **npm Workspaces**
   - Pros: Viene con Node.js — zero instalación extra. Flat `node_modules` por defecto (menos sorpresas).
   - Cons: Flat `node_modules` causa dependencias fantasma. Sin protocolo `workspace:*` nativo hasta npm 10. Performance inferior en instalaciones grandes. No maneja bien monorepos con dependencias circulares.

3. **Yarn Workspaces (Yarn Berry)**
   - Pros: PnP (Plug'n'Play) elimina `node_modules` por completo. Zero-installs para CI. Caching avanzado.
   - Cons: PnP rompe muchos paquetes del ecosistema. Curva de aprendizaje alta. El ecosystem está migrando pero no es estable al 100%. Problemas históricos con compatibilidad.

4. **Nx o Turborepo (meta-framework sobre pnpm)**
   - Pros: Caching de builds inteligente. Affected-test — solo corre lo que cambió. Topological sort automático. Dashboards de CI.
   - Cons: Overkill para 3 paquetes. Capa de abstracción adicional. Configuración compleja (nx.json, project.json). Incremento significativo en el mantenimiento. Sprint 1 no tiene builds paralelos ni pipelines complejos que lo justifiquen.

## Decision

Elegimos **pnpm Workspaces** sin meta-framework. El `pnpm-workspace.yaml` define `apps/*` y `packages/*` como paquetes del workspace. Los scripts en el root `package.json` usan `--filter` para objetivos específicos (`pnpm dev:api`, `pnpm dev:web`). La dependencia `@legacy/shared` se referencia con `"workspace:*"` en los package.json de api y web.

Si en futuros sprints el monorepo crece significativamente (+10 paquetes) o los tiempos de build se vuelven un cuello de botella, podemos evaluar la adopción de Nx o Turborepo como capa superior sin cambiar la base pnpm.

## Consequences

- **Easier**: Aislamiento estricto por defecto — un paquete no accede a dependencias de otro accidentalmente. Symlinks automáticos para dependencias internas. Instalaciones rápidas con content-addressable store. Simplicidad: un archivo `pnpm-workspace.yaml` define todo.
- **Harder**: Requiere `--filter` para scripts cross-package (no hay "corre todo automáticamente" como Nx). Sin caching de builds — `pnpm build` siempre corre completo. Los symlinks pueden confundir a tooling que no los espera.
- **New constraints**: Todas las dependencias internas deben usar `workspace:*`. No se usa `file:` protocol. Los scripts de root son conveniencia, no reemplazo de `--filter` en CI. Si se adopta Nx/turborepo después, la migración es incremental.
- **Migration path**: Sprint 1 establece el workspace. Sprint 2 agrega `@legacy/shared` como dependencia real de api y web. Futuros sprints evalúan Nx si el monorepo crece.